.profileEditItemContainer {
  width: 100%;
  height: 34px;
  display: flex;
  justify-content: space-between;
  align-items: center;
  border-bottom: 1px solid #EAEAEA;
  position: relative;
}

.editProfileInput {
  position: absolute;
  right: 0;
  text-align: end;
  color: var(--color-secondary);
  
  &:active, &:focus {
    color: inherit;
  }
}
